# WoofBnB frontend environment variables
# Only VITE_-prefixed values are exposed to the browser. Never place secrets here.

# Base URL of the WoofBnB REST API, including the version prefix.
VITE_API_BASE_URL=/api/v1

# Request timeout for API calls, in milliseconds.
VITE_API_TIMEOUT_MS=15000
